export enum GenderType {
  MALE,
  FEMALE,
  OTHERS,
}

export enum AssetLifeStatus {
  ALL = 'ALL',
  DEAD = 'DEAD',
  LIVE = 'LIVE',
}

export enum ImportTaskStatus {
  DONE = 'DONE',
  FAILED = 'FAILED',
  PROCESSING = 'PROCESSING',
  QUEUE = 'QUEUED',
}
